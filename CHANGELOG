# Version 1.0.0
* Refactored all modules into spyts module.
* config now parsed as yaml.

# Version 0.4.0
* CHANGELOG added
* extended tweets now retrieved -- tweet text in "full_text" instead of "text"
* "quoted_status" purged from output now (could recursively explode if expanded)
